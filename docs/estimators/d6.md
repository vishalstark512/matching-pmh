# D6 — Temporal / sensor residual (Lemma D6)

**When:** label-constant drift along sequences ($\Delta h$ between frames).

```python
# sequences: [N, T, d] or precomputed residuals [N, d]
artifact = estimate_from_config(SigmaTaskConfig.for_temporal(), sequences)
```
