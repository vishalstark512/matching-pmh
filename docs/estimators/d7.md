# D7 — Alignment / adversarial Gram (Lemma D7)

**When:** style rewrites or PGD deltas define deployment variation (T7A/T7B).

**Embedding deltas already computed:**

```python
artifact = estimate_from_config(
    SigmaTaskConfig.for_alignment(rank=128),
    deltas,  # [M, d]
)
```

**Style-pair JSON records + encoder:**

```python
pairs = [{"prompt": "...", "content_fixed": "...", "style_variants": {"bulleted": "..."}}]
artifact = estimate_from_config(
    cfg,
    pairs,
    encoder=lambda texts: model.encode(texts),
)
```
