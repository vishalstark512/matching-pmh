# D4 — Domain Gram (Lemma D4)

**When:** paired or unpaired source/target domains shift representation means (T4 vision, features).

```python
cfg = SigmaTaskConfig.for_domain(rank=64)
artifact = estimate_from_config(cfg, feats_source, feats_target)
```

**Vision:** per-layer Gram + `MultiLayerPMHLoss` — see [Training](../training.md) and `examples/07_vision_multilayer.py`.

**Collect features from a loader:**

```python
from pmh import collect_features
h_src = collect_features(backbone, source_loader, max_batches=50)
```
