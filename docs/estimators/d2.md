# D2 — Isotropic nuisance (Lemma D2)

**When:** deployment noise has no preferred direction (sensor noise, $\mathcal{N}(0,\sigma^2 I)$).

```python
cfg = SigmaTaskConfig.for_isotropic(dim=768, noise_level=0.1)
artifact = estimate_from_config(cfg)  # no feature tensors needed
```

**PMH:** $\Sigma' = \sigma^2 I$ — compare to matched arm to test whether direction matters.
