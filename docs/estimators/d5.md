# D5 — Compositional nuisance (Lemma D5)

**When:** nuisance lives in known coordinates (atoms, token groups).

```python
cfg = SigmaTaskConfig.for_compositional(nuisance_indices=[0, 1, 2, 3, 4])
artifact = estimate_from_config(cfg, features)  # [N, d]
```
