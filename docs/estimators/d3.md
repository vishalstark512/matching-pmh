# D3 — Augmentation modes (Lemma D3)

**When:** training uses a finite set of label-preserving augmentations with coefficients $\beta_k$.

```python
# aug_deltas: [K, d] mean feature delta per mode, or [K, N, d]
artifact = estimate_from_config(
    SigmaTaskConfig.for_augmentation(),
    aug_deltas,
)
```
