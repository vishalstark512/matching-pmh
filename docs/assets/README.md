# Docs assets

Optional media for the README and docs site.

| File | Purpose |
|------|---------|
| `first_run_demo.gif` (not in repo yet) | ~20s: `pmh-train route --wizard` + `python scripts/demos/first_run_domain_shift.py` |

Until a GIF exists, use [DEMO_OUTPUT.md](../DEMO_OUTPUT.md) for expected terminal text.

**Record locally:**

1. Terminal font 14–16px, dark theme.
2. Run `pmh-train route --wizard --non-interactive --stack pytorch`.
3. Run `python scripts/demos/first_run_domain_shift.py`.
4. Export GIF (e.g. ScreenToGif, Peek) and add here; link from README.
