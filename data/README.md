# Local data directory (gitignored)

Put downloaded datasets here for local runs only (e.g. Office-31).  
**Nothing under `data/` is committed to GitHub or published on PyPI.**

See [docs/DATA_POLICY.md](../docs/DATA_POLICY.md).
